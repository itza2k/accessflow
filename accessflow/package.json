{
  "name": "accessflow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node src/icons/create-icons.js",
    "build": "vite build",
    "watch": "vite build --watch",
    "package": "cd dist && zip -r ../accessflow.zip .",
    "build:package": "npm run build && npm run package",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vitest": "^1.2.0"
  }
}
